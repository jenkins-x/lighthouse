kind: Role
apiVersion: rbac.authorization.k8s.io/v1beta1
metadata:
  name: {{ template "tide.name" . }}
rules:
  - apiGroups:
      - jenkins.io
    resources:
      - pipelineactivities
      - sourcerepositories
    verbs:
      - list
      - watch