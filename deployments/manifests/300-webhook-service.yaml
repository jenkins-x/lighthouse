apiVersion: v1
kind: Service
metadata:
  name: webhook
  namespace: lighthouse
  annotations:
    app: webhook
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: webhook
  sessionAffinity: None
  type: ClusterIP
